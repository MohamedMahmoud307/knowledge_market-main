# knowledge_market

منصة أسئلة وتصويت مبنية بـ PHP و MySQL.

## 📌 وصف المشروع
مشروع ويب بسيط يسمح للمستخدمين بإضافة أسئلة، التصويت عليها، تعديلها أو حذفها. يحتوي على لوحة مدير، صفحة رئيسية، ملف شخصي، وصفحة لأكثر الأسئلة تصويتًا. تم تطويره باستخدام PHP و MySQL ويعمل على بيئة XAMPP.

## 🛠️ التقنيات المستخدمة
- PHP 7+
- MySQL
- HTML/CSS
- XAMPP (Apache + MySQL)

## 📁 ملفات المشروع
- `index.php` – الصفحة الرئيسية  
- `login.php` – تسجيل الدخول  
- `register.php` – تسجيل مستخدم جديد  
- `profile.php` – عرض الأسئلة الخاصة بالمستخدم  
- `add_question.php` – إضافة سؤال جديد  
- `edit_question.php` – تعديل سؤال  
- `delete_question.php` – حذف سؤال  
- `top_questions.php` – عرض أكثر الأسئلة تصويتًا  
- `admin.php` – لوحة المدير  
- `vote.php` – التصويت على الأسئلة  
- `logout.php` – تسجيل الخروج  
- `db.php` – الاتصال بقاعدة البيانات  
- `style.css` – تصميم الصفحات  
- `knowledge_market.sql` – ملف قاعدة البيانات  

## 🧪 خطوات التشغيل
1. شغّل XAMPP → Apache + MySQL  
2. افتح `phpMyAdmin` وأنشئ قاعدة بيانات باسم `knowledge_market`  
3. استورد ملف `knowledge_market.sql` داخلها  
4. ضع ملفات المشروع داخل مجلد `htdocs/knowledge_market`  
5. افتح المتصفح واكتب:  http://localhost/knowledge_market 
منصة أسئلة وتصويت مبنية بـ PHP وMySQL 
## 👤 صلاحيات المستخدمين
- المستخدم العادي: يمكنه التسجيل، تسجيل الدخول، إضافة أسئلة، التصويت، تعديل وحذف أسئلته فقط.  
- المدير: لديه صلاحية الوصول إلى لوحة التحكم، إدارة الأسئلة والمستخدمين.  

## 🗃️ هيكل قاعدة البيانات
### جدول `users`
- `id` – رقم المستخدم  
- `username` – اسم المستخدم  
- `password` – كلمة المرور  
- `is_admin` – هل المستخدم مدير (0 أو 1)  

### جدول `questions`
- `id` – رقم السؤال  
- `user_id` – صاحب السؤال  
- `question` – نص السؤال  
- `category` – التصنيف  
- `votes` – عدد التصويتات الإيجابية  
- `downvotes` – عدد التصويتات السلبية  
- `created_at` – تاريخ الإضافة  

### جدول `votes_log`
- `id` – رقم التصويت  
- `user_id` – من قام بالتصويت  
- `question_id` – السؤال المصوّت عليه  
- `vote_type` – نوع التصويت (up/down)  
 👤 Developed by: Mohamed Mahmoud Mohamed Ali
